# Colab evaluation bundle

This folder keeps the portable files used for the Google Colab evaluation workflow:

- `iskolarchat_eval_colab.ipynb` — evaluation notebook
- `iskolarchat_backend_for_colab.zip` — backend bundle uploaded by the notebook user
- `eval_results.zip` — downloaded evaluation output snapshot

The notebook expects the two ZIP filenames exactly as listed above.
